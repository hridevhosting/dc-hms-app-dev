import { DoctorSisterNotesDirective } from './doctor-sister-notes.directive';

describe('DoctorSisterNotesDirective', () => {
  it('should create an instance', () => {
    const directive = new DoctorSisterNotesDirective();
    expect(directive).toBeTruthy();
  });
});
