import { DevResIdentifyDirective } from './dev-res-identify.directive';

describe('DevResIdentifyDirective', () => {
  it('should create an instance', () => {
    const directive = new DevResIdentifyDirective();
    expect(directive).toBeTruthy();
  });
});
