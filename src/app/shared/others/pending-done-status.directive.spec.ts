import { PendingDoneStatusDirective } from './pending-done-status.directive';

describe('PendingDoneStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new PendingDoneStatusDirective();
    expect(directive).toBeTruthy();
  });
});
