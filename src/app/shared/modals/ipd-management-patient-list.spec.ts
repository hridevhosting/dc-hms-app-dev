import { IpdManagementPatientList } from './ipd-management-patient-list';

describe('IpdManagementPatientList', () => {
  it('should create an instance', () => {
    expect(new IpdManagementPatientList()).toBeTruthy();
  });
});
