import { IpdManagementPatientFilterList } from './ipd-management-patient-filter-list';

describe('IpdManagementPatientFilterList', () => {
  it('should create an instance', () => {
    expect(new IpdManagementPatientFilterList()).toBeTruthy();
  });
});
