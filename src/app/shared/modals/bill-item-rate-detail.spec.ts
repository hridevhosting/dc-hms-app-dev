import { BillItemRateDetail } from './bill-item-rate-detail';

describe('BillItemRateDetail', () => {
  it('should create an instance', () => {
    expect(new BillItemRateDetail()).toBeTruthy();
  });
});
